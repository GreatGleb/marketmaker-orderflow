# Исправления после финального аудита

Начало: 2026-09-18 16:24:45 UTC. Исправляем актуальность донора перед
входом и ограничения объёма v3. REST остаётся без изменений; прогон
остановлен, боевой бот выключен.

Резервная копия до изменения логики:
`/tmp/orderflow-audit-final-20260918/live-final.dump`.
Снимок не изменился: перед правками повторно проверено 14 150 сделок и
486 256 котировок. Исходники до исправлений сохранены там же в `source.tar.gz`.
Новые сделки после исправлений относятся к новому эксперименту;
смешивать их с сохранённым прогоном нельзя.


Результат: отзыв/истечение/замена донора запрещают новый вход, включая
цепочки v2/v3, повтор после таймаута и ожидание без цены. TTL публикации
90 секунд, проверка при ожидании раз в секунду; цепочка v2/v3 пересчитывается
раз в 30 секунд и на сигнале. Обновление того же конфига сохраняет ожидание.
Рейтинг `WindowCache` сохраняет прежний срок; открытая позиция не меняется.

Объём v3 проверяется по обеим сеткам/границам количества и минимальному
номиналу, на Decimal. Расчёт по фактической цене сигнала выполняется до
открытия; номинал позиции дальше зафиксирован. Новая проверка не использует
mark price: в текущем симуляторе его нет, номинал оценивается по цене входа.
Это не подтверждение принятия заявки настоящей биржей.

Проверки:

- 30 из 30 тестовых модулей без ошибок и пропусков на отдельных хранилищах.
  Финальные дополнительные проверки отмены задачи и 256 сеток также прошли.
- Повтор исходного `reproduce_order_limits.py` на сохранённой рабочей БД,
  только чтение: BMT при балансе $5/$4/$1 отклонён по номиналу; $10 даёт
  522 единицы на $9.88668. Принятых превышений рыночного максимума — 0.
- Журналы: `/tmp/orderflow-fixes-20260918/tests.jsonl`, `regressions.log`.
  Сводка: [test-summary.json](test-summary.json), повтор воспроизведения:
  [order-limit-verification.json](order-limit-verification.json).

Источник REST оставлен по решению пользователя; восстановление фьючерсного
потока отложено до продакшн-сервера. Живой прогон не возобновлялся.

Финальная проверка сохранности: 14 150 сделок, 486 256 котировок — без
изменений. `supervisorctl status`: все три шарда, питатель, воркеры и
вставка сделок STOPPED; FastAPI работает; `binance_bot` не запускался.
Временный тестовый Redis удалён.


Повторная проверка по просьбе пользователя выявила и устранила гонку
между сигналом цены и финальным запросом цепочки донора. После неё прошли
**31 из 31** модулей без пропусков, включая TTL на настоящем Redis.
Подробности: [recheck.md](recheck.md), [recheck-tests.json](recheck-tests.json).
